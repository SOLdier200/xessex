import LaunchClient from "./LaunchClient";

export const metadata = {
  title: "Launch | Xessex",
  description: "XESS Token Launch - Private & Public Sale",
};

export default function LaunchPage() {
  return <LaunchClient />;
}
