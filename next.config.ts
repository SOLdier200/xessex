import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack config for Next.js 16+
  turbopack: {
    // Empty config to use Turbopack without webpack fallback
  },
};

export default nextConfig;
